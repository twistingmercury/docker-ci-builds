package main_test

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestExample(t *testing.T) {
	// This test is only to demonstrate running of unit tests as
	// part of the CI docker build.
	assert.Equal(t, 0, 0)
}
